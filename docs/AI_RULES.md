# AI Rules

## TypeScript
- For any modified TypeScript file, check for:
  - unused variables
  - unused parameters
  - unused imports
- Prefer fixing locally (remove or rename to `_var`).
- Do not run or suggest full TypeScript project builds unless asked.
