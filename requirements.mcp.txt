# MCP server dependency — layered on top of the existing app image (see
# Dockerfile.mcp), so it does NOT alter the bot/webapp requirements.txt pins.
# Installing on top of the base image lets pip bump shared transitive deps
# (e.g. starlette) only inside the zana-mcp image if the SDK requires it.
mcp>=1.12,<2.0
